server:
  port: 8082
  
spring:   application:    name: HOTEL-SERVICES
  datasource:
    url: jdbc:postgresql://localhost:3030/microservice?    username: postgres 
    password: Sachin@123
    
      jpa:    hibernate:      ddl-auto: update
    show-sql: true
   
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
